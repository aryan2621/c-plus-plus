// c++ headers and the operators;
// by using the header file we enhance our programming
// by using the resource that are available inside these files

// types of header files
// system header files -> comes with compilers
// user defiend header files -> these are written by programmers

#include <iostream>
// cpp reference a website for the c++ reference
// system header file
// #include "this.h" a way of defining the  header file of user defined type

using namespace std;

int main()
{
    int a = 3, b = 4;
    cout << a + b << endl;
    cout << a - b << endl;
    cout << a * b << endl;
    cout << a / b << endl;
    cout << a % b << endl;
    cout << a + b << endl;
    cout << a++;
    cout << a--;
    cout << --a;
    cout << ++a;

    //integer and integer arithmetic operation gives integer only;


    //  asignment operators-> use to assign value to operators
    // =


    // comparsion operators->for comparing the values == = != >= <= > < 
    // gives the value as boolean



    // logical operators-> and or not;
    // && || !
    // if both true then overall true  and
    // if any true then overall true or
    // not ... just reverse as opposite 

    
    return 0;
}

//operators -> endl for new line
// types of operators in c++;
// arithmetic operators +/-///*