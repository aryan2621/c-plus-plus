#include <iostream>
#include <iomanip>

int main(int argc, char const *argv[])

{

    const int a = 9;

    // in c++ the variables are mutables
    // but you can make them immutable
    // by makng them as contants.
    // create it as only read only variable.
    // just by placing the const keyword in front of them.

    // manipulators-> helps in data display formatting
    // endl
    // iomanip contain a function as setw(4) provide a space of four characters

    // operators precedence
    // take help of cpp reference
    // int c=(((a*5)+b)-45);
    // for same precedence goes from left to right
    // for different it goes from top to down

    return 0;
}