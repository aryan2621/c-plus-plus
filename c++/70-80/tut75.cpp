// string class and string funnctions

#include <iostream>
#include <string>
using namespace std;

int main()
{
    // string bucky;
    // cin>>bucky;

    // string x;

    // it will take the whole line until we enter the enter
    // getline(cin, x);

    // cout << x << endl;

    // important to know that
    // in keyword entered rishabh is a good boy
    // in screen it is rishabh
    // as cin ignore the spaces

    // cout<<"The string entered is "<<bucky;

    // string s1("rishabh");
    // it will copy the rishabh in s1;

    // blank string;
    // string s2, s3;

    // copy string

    // s2 = s1;
    //this will also copy

    // s3.assign(s1);
    // cout << s2;

    // string are likely most the array of characters

    // string s1="rishabh";
    // cout<<s1.at(3)<<endl;
    // for (int i = 0; i < s1.length(); i++)
    // {
    //     cout<<s1.at(i)<<" ";
    // }

    // starting index and then uske age kaha tak ko chhatna hai;

    // string s("ishabh i think i m a super human");
    // cout << s.substr(3, 7) << endl;

    // swaping

    // string one("apples");
    // string two("banana");
    // cout << one << " " << two << endl;
    // one.swap(two);
    // cout << one << " " << two << endl;

    // string s1("ham is span is not a good am");
    // cout << s1.find("am") << endl;
    // it returns index of occurance
    // first occurance

    // cout << s1.rfind("am") << endl;
    //last occurance

    // earse from that particular index

    // string s1("hi my name is bucky and u kive abananan");
    // cout << s1 << endl;
    // s1.erase(20);
    // cout << s1 << endl;

    // replace with the given set of characters

    // cout << s1 << endl;
    // s1.replace(14, 5, "rishabh");
    // cout << s1 << endl;

    // inserting the characters

    // string s1("hii this is rishabh");
    // cout << s1 << endl;
    // s1.insert(12, "anamika ");
    // cout << s1 << endl;

    // string s1("");

    return 0;
}